services:
  app:
    image: uswccr.ccs.tencentyun.com/inpolar/own-workflow:main
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8000}:8000"
    restart: unless-stopped
    volumes:
      - ./var:/app/var
